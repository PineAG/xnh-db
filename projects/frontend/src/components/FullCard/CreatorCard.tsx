import { CreatorExport } from "@xnh-db/types"
import { CardComponent } from "./utils"

export const CreatorCard: CardComponent<CreatorExport> = (props: {item: CreatorExport}) => {
    return <div>施工中</div>
}