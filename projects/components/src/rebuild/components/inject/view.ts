import { FieldConfig } from "@xnh-db/protocol";

export module EndpointViewers {
    export function renderEndpoint<T extends FieldConfig.Fields.EndpointValueTypes>(value: T | undefined, config: FieldConfig.Fields.EndpointTypes): React.ReactNode {

    }
}