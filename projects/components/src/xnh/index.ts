import {config as internalConfig} from "./config"
import {layouts as internalLayouts} from "./layouts"

export module XnhUiConfiguration {
    export const config = internalConfig
    export const layouts = internalLayouts  
}